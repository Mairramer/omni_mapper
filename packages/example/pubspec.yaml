name: example
description: Example project for omni_mapper.
publish_to: 'none'

environment:
  sdk: '>=3.12.0 <4.0.0'

resolution: workspace

dependencies:
  omni_mapper: ^0.5.0

dev_dependencies:
  build_runner: ^2.4.0
  omni_mapper_generator: ^0.5.0
